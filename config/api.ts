// Your computer's local network IP, so the phone (on the same WiFi) can reach
// the FastAPI server running on your machine. Update this if your IP changes.
export const API_BASE_URL = 'http://192.168.0.224:8000';
